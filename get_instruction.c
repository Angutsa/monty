#include <stdio.h>

/**
  * get_instruction - gets and executes the instruction
  * @line: entire line from monty file
  * @linenumber: line number of the opcode to execute
  */
void get_instruction(char *line, int linenumber)
{
	printf("Line %d: %s\n", linenumber, line);
	/** TODO: execute get_instruction */
}
